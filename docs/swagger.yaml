openapi: 3.0.3
info:
  title: Kasir API
  description: API documentation for products and categories.
  version: 1.0.0
servers:
  - url: /
paths:
  /health:
    get:
      tags:
        - Health
      summary: Health check
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HealthResponse"
  /api/products:
    get:
      tags:
        - Products
      summary: List products
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/WebResponseProductListArray"
        "500":
          description: Internal Server Error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
    post:
      tags:
        - Products
      summary: Create product
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateProductRequest"
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/WebResponseProductDetail"
        "400":
          description: Bad Request
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "500":
          description: Internal Server Error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/products/{id}:
    get:
      tags:
        - Products
      summary: Get product by ID
      parameters:
        - $ref: "#/components/parameters/IdPathParam"
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/WebResponseProductDetail"
        "400":
          description: Invalid ID
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "404":
          description: Not Found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
    put:
      tags:
        - Products
      summary: Update product
      parameters:
        - $ref: "#/components/parameters/IdPathParam"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateProductRequest"
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/WebResponseProductDetail"
        "400":
          description: Bad Request
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "404":
          description: Not Found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "500":
          description: Internal Server Error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
    delete:
      tags:
        - Products
      summary: Delete product
      parameters:
        - $ref: "#/components/parameters/IdPathParam"
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/WebResponseMessage"
        "400":
          description: Invalid ID
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "404":
          description: Not Found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "500":
          description: Internal Server Error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/categories:
    get:
      tags:
        - Categories
      summary: List categories
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/WebResponseCategoryResponseArray"
        "500":
          description: Internal Server Error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
    post:
      tags:
        - Categories
      summary: Create category
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateCategoryRequest"
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/WebResponseCategoryResponse"
        "400":
          description: Bad Request
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "500":
          description: Internal Server Error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/categories/{id}:
    get:
      tags:
        - Categories
      summary: Get category by ID
      parameters:
        - $ref: "#/components/parameters/IdPathParam"
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/WebResponseCategoryResponse"
        "400":
          description: Invalid ID
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "404":
          description: Not Found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
    put:
      tags:
        - Categories
      summary: Update category
      parameters:
        - $ref: "#/components/parameters/IdPathParam"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateCategoryRequest"
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/WebResponseCategoryResponse"
        "400":
          description: Bad Request
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "404":
          description: Not Found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "500":
          description: Internal Server Error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
    delete:
      tags:
        - Categories
      summary: Delete category
      parameters:
        - $ref: "#/components/parameters/IdPathParam"
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/WebResponseMessage"
        "400":
          description: Invalid ID
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "404":
          description: Not Found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "500":
          description: Internal Server Error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
components:
  parameters:
    IdPathParam:
      name: id
      in: path
      required: true
      schema:
        type: integer
        format: int64
  schemas:
    HealthResponse:
      type: object
      properties:
        status:
          type: string
          example: ok
    ProductListResponse:
      type: object
      properties:
        id:
          type: integer
          format: int64
          example: 1
        name:
          type: string
          example: "Product A"
        price:
          type: number
          format: double
          example: 25000.50
        stock:
          type: integer
          format: int64
          example: 100
    ProductDetailResponse:
      type: object
      properties:
        id:
          type: integer
          format: int64
          example: 1
        name:
          type: string
          example: "Product A"
        price:
          type: number
          format: double
          example: 25000.50
        stock:
          type: integer
          format: int64
          example: 100
        category:
          allOf:
            - $ref: "#/components/schemas/ProductCategoryResponse"
          nullable: true
    ProductCategoryResponse:
      type: object
      properties:
        id:
          type: integer
          format: int64
          example: 1
        name:
          type: string
          example: "Electronics"
    CategoryResponse:
      type: object
      properties:
        id:
          type: integer
          format: int64
          example: 1
        name:
          type: string
          example: "Electronics"
        description:
          type: string
          nullable: true
          example: "Electronic devices and accessories"
    CreateProductRequest:
      type: object
      required:
        - category_id
        - name
        - price
        - stock
      properties:
        category_id:
          type: integer
          format: int64
          example: 1
        name:
          type: string
          example: "Product A"
        price:
          type: number
          format: double
          minimum: 0
          example: 25000.50
        stock:
          type: integer
          format: int64
          minimum: 0
          example: 100
    UpdateProductRequest:
      type: object
      required:
        - category_id
        - name
        - price
        - stock
      properties:
        category_id:
          type: integer
          format: int64
          example: 1
        id:
          type: integer
          format: int64
          description: Optional. Server uses the path ID.
          example: 1
        name:
          type: string
          example: "Product A Updated"
        price:
          type: number
          format: double
          minimum: 0
          example: 30000.75
        stock:
          type: integer
          format: int64
          minimum: 0
          example: 150
    CreateCategoryRequest:
      type: object
      required:
        - name
      properties:
        name:
          type: string
          example: "Electronics"
        description:
          type: string
          nullable: true
          example: "Electronic devices and accessories"
    UpdateCategoryRequest:
      type: object
      required:
        - name
      properties:
        id:
          type: integer
          format: int64
          description: Optional. Server uses the path ID.
          example: 1
        name:
          type: string
          example: "Electronics Updated"
        description:
          type: string
          nullable: true
          example: "Updated description for electronics"
    MessageResponse:
      type: object
      properties:
        message:
          type: string
          example: "Operation successful"
    WebResponseProductListArray:
      type: object
      properties:
        data:
          type: array
          items:
            $ref: "#/components/schemas/ProductListResponse"
        errors:
          type: string
    WebResponseProductDetail:
      type: object
      properties:
        data:
          $ref: "#/components/schemas/ProductDetailResponse"
        errors:
          type: string
    WebResponseCategoryResponseArray:
      type: object
      properties:
        data:
          type: array
          items:
            $ref: "#/components/schemas/CategoryResponse"
        errors:
          type: string
    WebResponseCategoryResponse:
      type: object
      properties:
        data:
          $ref: "#/components/schemas/CategoryResponse"
        errors:
          type: string
    WebResponseMessage:
      type: object
      properties:
        data:
          $ref: "#/components/schemas/MessageResponse"
        errors:
          type: string
    ErrorResponse:
      type: object
      properties:
        status:
          type: integer
          format: int64
        message:
          type: string
